/**
 * Settings for Layout 1
 * @type {{leftSidebar: {mode: string, bgImgURL: string, show: boolean, theme: string}}}
 */

const Layout1Settings = {
  leftSidebar: {
    show: true,
    mode: 'full',
    theme: 'slateDark1',
  }
}

export default Layout1Settings;